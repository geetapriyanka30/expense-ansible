- name: mysql install
  hosts: local
  connection: local
  become: yes
  tasks: 
  - name: Install mysql
    ansible.builtin.package:
     name: mysql-server
     state: present

  - name: run mysql
    ansible.builtin.service:
     name: mysql-server
     state: started
     enabled: yes

  