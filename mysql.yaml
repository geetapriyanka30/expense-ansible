- name: mysql install
  hosts: local
  connection: local
  become: yes
  tasks: 
  - name: Install mysql
    ansible.builtin.package:
     name: mysql-server
     state: present

  - name: run mysql
    ansible.builtin.service:
     name: mysqld
     state: started
     enabled: yes

  